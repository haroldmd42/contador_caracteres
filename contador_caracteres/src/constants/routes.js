/**
 * Centralized route definitions for the application.
 * Single source of truth for all navigation paths.
 */
export const ROUTES = {
  HOME: '/',
  CHARACTER_COUNTER: '/contador',
  FILE_LIBRARY: '/generador',
  ENCODER: '/encoder',
  IMAGE_BASE64: '/image-tools',
  IMAGE_RESIZER: '/image-resizer',
};
